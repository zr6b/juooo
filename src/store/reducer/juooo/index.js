import juoooType from '../../actionType/juooo'
export default function (state=inisState,{type,payload}){
    state=JSON.parse(JSON.stringify(state));
    if (type === juoooType.UP_CLASSIFY_HOME){
    }
}